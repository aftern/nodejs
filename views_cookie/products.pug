doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/products') Products List
        ul
            each cart in carts
                li
                    = cart.title +' '
                    a(href='/cart/'+cart.id) add
        article
            a(href='/cart') cart