doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/cart') Cart
        ul
            each cart in carts
                li
                    = cart.title +'('+cart.count+')'
        article
            a(href='/products') products list