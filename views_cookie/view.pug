doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/products') Products List
        ul
            each cart in carts
                li
                    a(href='/cart/'+cart.id)= cart.title
        article
            if cart
                a(href='/cart') cart
            else    
                h2 Welcome
                | This is server side javascript tutorial.